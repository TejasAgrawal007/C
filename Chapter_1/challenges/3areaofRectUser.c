#include<stdio.h>

int main(){
    int length, bredth;
    printf("What is the length of Rectangle \n");
    scanf("%d", &length);

    printf("What is the bredth of rectangel \n");
    scanf("%d", &bredth);

    printf("The area of Your Rectangle is %d", length*bredth);
    return 0;
}