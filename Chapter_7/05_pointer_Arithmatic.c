#include <stdio.h>

int main()
{
    // int i = 34;
    // int *ptr = &i;

    // printf("The before value of of ptr is %u\n", ptr);
    // ptr++;
    // ptr++;
    // printf("The after value of of ptr is %u\n", ptr);


    int  i = 34;
    int *ptr = &i;


    printf("The Before Increment Value of i address is %u\n", ptr);
    ptr++;
    printf("The After Increment Value of i address is %u\n", ptr);





    return 0;
}